export default config;
declare namespace config {
    const id: null;
    namespace options {
        const accurateTrackBounce: boolean;
        const clickmap: boolean;
        const defer: boolean;
        const ecommerce: boolean;
        const params: never[];
        const userParams: {};
        const trackHash: boolean;
        const trackLinks: boolean;
        const type: number;
        const webvisor: boolean;
        const triggerEvent: boolean;
    }
    const router: null;
    const env: string;
    const scriptSrc: string;
    const debug: boolean;
    const ignoreRoutes: never[];
    const skipSamePath: boolean;
}
